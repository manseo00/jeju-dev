# Neon PostgreSQL 연결 문자열
# Neon 대시보드 > Connection Details 에서 복사한 값을 사용하세요.
# Neon 은 SSL 이 필요하므로 반드시 sslmode=require 를 포함해야 합니다.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
